@model IEnumerable<Hurtownia.Models.ViewModels.StatsViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>


<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Employee.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Employee.Surname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Employee.Login)
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.Surname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.Login)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Orders.First())
        </td>
        @*<td>
            @foreach (var product in item.ProductLists)
            {
                <td>@product.Product.Name + @((product.Product.Amount * product.Product.Price).ToString("c"))</td>
            }
        </td>*@
    </tr>
}

</table>
